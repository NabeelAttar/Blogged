import React from 'react';
function Logo({ width = "140px" }) {
  return (
    <div className="font-display text-2xl font-bold bg-gradient-to-r from-brand-gold via-brand-gold2 to-brand-gold3 bg-clip-text text-transparent tracking-wide">
      BLOGGED
    </div>
  )
}
export default Logo;
